#include "cm_unmount.h"

cm_unmount::cm_unmount()
{

}

void cm_unmount::ejecutar(){
    if(strlen(id) == 0){
        cout<<"Los parametros obligatorios no se han completado";
    }else{
        //Agregar ejecucion
    }
}
