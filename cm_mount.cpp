#include "cm_mount.h"

cm_mount::cm_mount()
{

}

void cm_mount::ejecutar(){
    if(path == "" || strlen(name) == 0){
        cout<<"Los parametros obligatorios no se han completado";
    }else{
        //Agregar ejecucion
    }
}
