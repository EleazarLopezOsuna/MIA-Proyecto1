#include "cm_rmgrp.h"

cm_rmgrp::cm_rmgrp()
{

}

void cm_rmgrp::ejecutar(){
    if(strlen(name) == 0){
        cout<<"Los parametros obligatorios no se han completado";
    }else{
        //Agregar ejecucion
    }
}
