#include "cm_rmusr.h"

cm_rmusr::cm_rmusr()
{

}

void cm_rmusr::ejecutar(){
    if(strlen(usr) == 0){
        cout<<"Los parametros obligatorios no se han completado";
    }else{
        //Agregar ejecucion
    }
}
