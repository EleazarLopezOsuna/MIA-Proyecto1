#include "cm_exec.h"

cm_exec::cm_exec()
{

}

void cm_exec::ejecutar(){
    if(path == ""){
        cout<<"Los parametros obligatorios no se han completado";
    }else{
        //Agregar ejecucion
    }
}
