#include "cm_loss.h"

cm_loss::cm_loss()
{

}

void cm_loss::ejecutar(){
    if(strlen(id) == 0){
        cout<<"Los parametros obligatorios no se han completado";
    }else{
        //Agregar ejecucion
    }
}
