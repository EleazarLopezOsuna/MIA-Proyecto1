#include "cm_mkdir.h"

cm_mkdir::cm_mkdir()
{
    p = false;
}

void cm_mkdir::ejecutar(){
    if(path == ""){
        cout<<"Los parametros obligatorios no se han completado";
    }else{
        //Agregar ejecucion
    }
}
