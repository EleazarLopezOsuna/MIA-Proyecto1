#include "cm_recovery.h"

cm_recovery::cm_recovery()
{

}

void cm_recovery::ejecutar(){
    if(strlen(id) == 0){
        cout<<"Los parametros obligatorios no se han completado";
    }else{
        //Agregar ejecucion
    }
}
