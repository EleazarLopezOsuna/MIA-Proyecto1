#include "cm_mv.h"

cm_mv::cm_mv()
{

}

void cm_mv::ejecutar(){
    if(path == "" || dest == ""){
        cout<<"Los parametros obligatorios no se han completado";
    }else{
        //Agregar ejecucion
    }
}
